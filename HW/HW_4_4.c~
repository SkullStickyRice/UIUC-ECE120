#include <stdio.h>
 
int main()
{
  int x=4;

    if (x > 0) {
    printf("x>0\n");
    if (x > 1) {
        printf("x>1\n");
        if (x < 4) {
            printf("x<4\n");
        }
    }
}
 
    return 0;
}
